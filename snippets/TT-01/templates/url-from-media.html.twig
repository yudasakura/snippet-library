{# Get the url from a media field #}

{# On a node field #}
{{ node.field_download_links|file_url }}

{# On a paragraph field #}
{{ paragraph.field_download_links|file_url }}

{# Option 2 #}
{{ file_url(paragraph.field_image.entity.field_media_image.entity.fileuri) }}